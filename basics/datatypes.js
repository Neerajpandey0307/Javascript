"use strict";// treat all js code to new version
// alert (3+3) this works on browser but there is different way to use alert in nodejs

let name ="neeraj"// this is string data type
let age =24 // this is integer datatype
let haveYouDoneYourVaccination = true // this is boolean datatype
let state; // this is undifined datatype

// there are some types of datatype in js which we learn as we proceed in our learning. For example : symbol ,null and etc.

//object
console.log(typeof undefined);
console.log(typeof(null))