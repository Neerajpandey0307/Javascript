// In conversion in js is if you want to change the type of an variable  Syntax = let {new_variable} = {name of a datatyoe you want to change it}{(oldAssinedVariable)}

let score = "33" // this is string datatype 
// but you want to do process on int 
let IntScore = Number(score)

console.log(typeof(score))